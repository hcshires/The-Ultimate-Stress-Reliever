<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/background"
    tools:context="com.csstudios.developers.theultimatestressreliever.MainActivity">

    <ImageButton
        android:id="@+id/activityButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/title_main"
        android:layout_alignParentBottom="true"
        android:contentDescription="@string/todo"
        app:srcCompat="@drawable/ic_sync_black_24dp" />

    <Button
        android:id="@+id/stressButton"
        android:layout_width="161dp"
        android:layout_height="wrap_content"
        android:layout_above="@+id/activityButton"
        android:layout_centerHorizontal="true"
        android:background="#037cbe"
        android:contentDescription="@string/stress_free_day"
        android:text="@string/StressFreeButton"
        android:textColor="#ffffff" />

    <TextView
        android:id="@+id/title_main"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="22dp"
        android:contentDescription="@string/got_stress_description"
        android:fontFamily="@font/segoepr"
        android:text="@string/got_stress"
        android:textAlignment="center"
        android:textColor="@android:color/holo_orange_dark"
        android:textIsSelectable="false"
        android:textSize="57sp"
        app:fontFamily="@font/segoepr" />

</RelativeLayout>